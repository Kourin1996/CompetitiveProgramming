#include <bits/stdc++.h>

char board[9][9];

int main() {
    std::string s;
    std::stringstream ss;

    int i, j, l;
    while(std::cin >> s, s != "#") {
        std::replace(s.begin(), s.end(), '/', ' ');
        ss << s;

        while(ss >> s) {

        }
    }
}